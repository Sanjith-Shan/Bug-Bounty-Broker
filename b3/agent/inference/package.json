{
  "name": "b3-inference",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "B3 inference sidecar — calls EigenCloud AI Gateway through the official @layr-labs/ai-gateway-provider so attestation-based JWT auth works without us reimplementing the KMS flow.",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@layr-labs/ai-gateway-provider": "^1.0.1",
    "ai": "^6.0.168"
  }
}
